# Connection Mapper

An interactive web application to visualize user sign-in locations from CSV logs on an interactive map.

This tool parses security logs, geolocates all IP addresses, and plots them, providing a clear visual overview of connection origins.
